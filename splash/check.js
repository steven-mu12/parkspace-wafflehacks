
function getInfo(){
    var roomCode = document.getElementById("roomCode").value

    /* check through mainJSON here */
    
    /* If good, create new, if not, return string "no room with such code" */
}

